mlm.controller('MainCtrl',
    ['$scope',
     '$location',
     '$route',
     '$window',
     '$rootScope',
     '$q',
     'cacheManager',
     'bLog',

    function ($scope,
             $location,
             $route,
             $window,
             $rootScope,
             $q,
             cacheManager,
             bLog
             ) {

        // EMPTY controller
        // TODO: to put some code

    }]);